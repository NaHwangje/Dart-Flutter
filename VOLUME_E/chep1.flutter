import 'package:flutter/material.dart';

void main() {
  runApp(
    const Center(
      child: Text(
        'hellow, world!',
        textDirection: TextDirection.ltr,
        style: TextStyle(
          fontSize: 32,
          color: Colors.white,
        ),
      ),
    ),
  );
}
